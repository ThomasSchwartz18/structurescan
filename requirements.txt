# sqlite3 (paper trading storage) is part of the Python standard library — no separate package needed.
requests>=2.31
pandas>=2.0
rich>=13.0

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0

pytest>=8.0
httpx>=0.27
